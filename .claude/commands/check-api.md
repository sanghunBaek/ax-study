# /check-api

GitHub 데이터셋(smok95/lotto)의 최신 회차를 확인합니다.

## 실행
`https://smok95.github.io/lotto/results/latest.json` 을 curl로 호출해서 최신 회차 정보를 가져와줘.

DB의 최신 회차와 비교해서 동기화가 필요한지도 알려줘.
동기화가 필요하면 `scripts/seed-from-github.js`를 실행하면 된다고 안내해줘.
