# /seed-db

동행복권 API에서 전체 회차 데이터를 DB에 초기 적재합니다.

개발 환경 세팅 시 한 번 실행하는 용도입니다.

## 실행
1. DB에 현재 몇 회차까지 적재됐는지 확인
2. 빠진 회차가 있으면 동행복권 API 순차 호출 (200ms 간격)
3. 전체 진행 상황과 최종 적재 결과 출력

`scripts/seed.js` 를 실행해줘.
완료 후 "총 N회차 적재 완료" 형태로 요약해줘.
